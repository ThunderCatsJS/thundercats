import Actions from './Actions';
import Cat from './Cat';
import Store from './Store';
import waitFor from './waitFor';

export default {
  Actions: Actions,
  Cat: Cat,
  Store: Store,
  waitFor: waitFor
};
